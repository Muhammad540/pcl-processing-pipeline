# Point Cloud Processor Configuration File
# Lines starting with # are comments
# Format: parameter_name = value

# Filtering Parameters
filterResolution = 0.2
minPoint = (-10, -5, -2, 1)
maxPoint = (30, 7, 1, 1)
enableCropBox = true

# Region Removal Parameters (e.g., like if you want to remove points covering the body of your vehicle/robot)
removeMinPoint = (-1.5, -1.7, -1, 1)
removeMaxPoint = (2.6, 1.7, -0.4, 1)
enableRegionRemoval = false

# Segmentation Parameters
maxIterations = 100
distanceThreshold = 0.3
obstacleCloudColor = (1.0, 0.0, 0.0)
groundPlaneColor = (0.0, 1.0, 0.0)

# Clustering Parameters
clusterTolerance = 1.0
minClusterSize = 20
maxClusterSize = 1000

# Visualization Parameters
visualizerTurnedON = true
renderSegementation = true
renderAxisAlignedBoxes = true
renderClusters = true 
renderOrientedBoxes = false